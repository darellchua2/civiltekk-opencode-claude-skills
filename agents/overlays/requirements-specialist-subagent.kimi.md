## Harness binding — Kimi Code

- **Headless mechanics**: no interactive clarification channel exists in a Kimi Code subagent run — the headless clause above applies verbatim.
- **Delegation syntax**: delegate by agent name via the Task tool — `image-analyzer-subagent` for image interpretation, `xlsx-specialist-subagent` for tabular exports, `explore` for repo scans. Agents install to `~/.kimi-code/agents/`.
