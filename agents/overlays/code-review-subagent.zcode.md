## Harness binding — ZCode

- **Delegation**: ZCode subagents cannot spawn subagents — do all delegation work inline (scans, parallel review, language-specific passes). Secondary agents named in this file are not launchable from here.
- **Project memory**: no memory tool — if the repo has a `LEARNINGS/` directory, grep/read entries bearing on the review topic before responding; otherwise proceed on the diff alone.
- **Skills**: invoke team skill packs with `$skill-name` when needed; note ZCode connects MCP servers only at session start.
