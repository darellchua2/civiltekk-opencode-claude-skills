## Harness binding — Kilo Code

- **Headless mechanics**: no interactive clarification channel exists in a Kilo subagent run — the headless clause above applies verbatim.
- **Delegation syntax**: delegate via Kilo's task tool or `@mention` — `image-analyzer-subagent` for image interpretation, `xlsx-specialist-subagent` for tabular exports, `explore` for repo scans. Callers must grant `permission.task` for each delegated agent.
