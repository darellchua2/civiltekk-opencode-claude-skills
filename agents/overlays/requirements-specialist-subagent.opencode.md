## Harness binding — OpenCode

- **Headless mechanics**: the `question` tool is deny'd in your frontmatter and is primary-session-only — that is the concrete form of the headless clause above. Never call it; never hallucinate `read_mcp_resource` as a user channel.
- **Delegation syntax**: use the Task tool — `subagent_type="image-analyzer-subagent"` for image interpretation, `subagent_type="xlsx-specialist-subagent"` for tabular exports, `subagent_type="explore"` for repo scans.
