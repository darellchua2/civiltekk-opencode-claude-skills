{
  "$comment": "Provider pack: Alpha Vantage official MCP (remote) for market/macro/commodities data with cited figures. Deep-merged by deploy/merge-packs.mjs when --enable-pack alpha-vantage (v2 shape: mcp.servers.<name>.disabled:false + permissions array rule). Requires ALPHA_VANTAGE_API_KEY in the environment (free tier: 25 req/day). Pilot-verify: upstream documents OAuth as primary; if the gateway rejects static Bearer keys, switch the base-config entry to the oauth object per Alpha Vantage's docs.",
  "mcp": {
    "servers": {
      "alpha-vantage": { "disabled": false }
    }
  },
  "permissions": [
    { "action": "alpha-vantage*", "resource": "*", "effect": "allow" }
  ]
}
